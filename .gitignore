# secrets — never commit
.claude/secrets.env
.env
.env.local
.env.local.*
# tracked, secret-free env templates are allowed explicitly
!backend/.env.prod
!backend/.env.staging
!backend/.env.dev
!backend/.env.local.dev

# local-only knowledge and tooling state
.loop_knowledge/
.claude/last-test-video.txt
**/*-workspace/
.claude/scratch/
.claude/worktrees/

# OS / editor / caches
.DS_Store
.pytest_cache/
__pycache__/
.vscode/

# local-only Claude Code state (settings.local.json holds machine permissions incl. secret-bearing patterns)
.claude/settings.local.json
.claude/learnings/
.cortex/atlas/sources/
.cortex/pulse/
.cortex/constellation.json
.cortex/archive/documents/*/source.*

# internal prose — plans, QA, handoffs, onboarding notes, vendored reference apps.
# Kept on disk, out of the public repo. Cortex knowledge lives in .cortex/ instead.
docs/

# Local iOS build output and watchman cookies: never commit (swept into #195 by mistake).
/build/
.watchman-cookie-*
