A metric that earns its place.

```jsx
<StatCard label="Taxa de ocupação" value="88%" sub="211 de 240 lugares" delta="+6%" bars={[58,72,64,80,76,92,100]} />
```

A bare number is banned: every metric carries either a denominator (`sub`) or a direction (`delta`). Three per screen, maximum.
