Text entry.

```jsx
<Input label="Nome" placeholder="Nome do jogador" />
<Input multiline label="Mensagem" defaultValue="Olá {nome}, tens vaga…" />
```

Message templates show `{nome}`, `{nível}`, `{dia}`, `{hora}` as literal braces — the coach edits them as plain text.
