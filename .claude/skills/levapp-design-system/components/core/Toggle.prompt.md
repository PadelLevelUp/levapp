Switch for settings and automation rules.

```jsx
<Toggle checked={rule.on} onChange={setOn} label="Reenviar sem resposta" />
```

Changes save on flip. Never pair a screen of toggles with a floating "Guardar alterações" bar.
