Filters above a list.

```jsx
<FilterPill active count={1}>Por responder</FilterPill>
<FilterPill count={8}>À espera</FilterPill>
```

Counts belong in the pill so the coach can see where the work is without switching.
