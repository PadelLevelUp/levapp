---
path: frontend/apps/web/src/integrations/supabase/client.ts
extracted_at: 2026-09-03T14:14:29Z
extraction_level: 3
size_lines: 17
size_tokens: 148
centrality: high
built_at_commit: "55cbb68fa2a12a87cf20a7025f6e94b9681f8226"
source_sha256: "b0ca77b4eaebb21dda36ab152d6b6b7f4718031a5bbec12d101ddb88967efd83"
---

## Purpose

Instantiates a Supabase client (`createClient<Database>`) from `VITE_SUPABASE_URL`/`VITE_SUPABASE_PUBLISHABLE_KEY` env vars, configured for localStorage-persisted, auto-refreshing sessions. **This is dead code**: a leftover from the project's original Lovable-generated prototype, before it was rebuilt on the Flask/`@levelup/api` backend this repo actually runs on now. A repo-wide search for `integrations/supabase` under `frontend/apps/web/src` and `frontend/packages` turns up zero imports of this file outside its own directory — nothing in the running app constructs or references `supabase`.

## Main players

- `export const supabase` (lines 11–16) — the sole export; a live Supabase JS client object that is never imported anywhere. Auto-generated per its header comment ("This file is automatically generated. Do not edit it directly.") — a Lovable/Supabase-tooling artifact, not hand-maintained app code.

## Insights

- Real auth/session state for this app flows entirely through `@levelup/api`'s `TokenStorage`/`initApi` (see `frontend/apps/web/src/api/client.ts`) and its own `localStorage["accessToken"]` key — completely separate from whatever this file's `persistSession`/`autoRefreshToken` config would do if it were ever wired up.
- Because nothing imports it, `VITE_SUPABASE_URL`/`VITE_SUPABASE_PUBLISHABLE_KEY` being unset in an environment has zero runtime effect on the app; `createClient` still executes at whatever time this module is first evaluated (never, if nothing imports it — tree-shaking or simple non-import means it likely never runs in the built bundle at all).
- A safe deletion candidate: removing `frontend/apps/web/src/integrations/` (this file and `types.ts`) plus the `@supabase/supabase-js` dependency would have no functional effect on the app, based on the zero-import finding above — worth flagging as a follow-up cleanup rather than acting on it here.

## Connections

Uses:
- `frontend/apps/web/src/integrations/supabase/types.ts`: imports the generated `Database` type to parameterize `createClient<Database>`.

Used by: no file in this scope, and (per the repo-wide grep) no file in the wider `frontend/` tree — orphaned.

## Query pointers

If you're deciding whether Supabase is live anywhere in this app: it isn't. All real API/session behavior is `@levelup/api`-based; treat any Supabase reference as prototype-era dead code, not a second backend.
