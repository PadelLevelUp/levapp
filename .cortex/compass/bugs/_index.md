# Bug ledger — index

**Read this when:** a bug is reported, a test fails unexpectedly, or you need to know
whether a failure mode has been seen before.

**What's here:**
- `B-NNN-<slug>.md` — one file per bug: type (seven-type taxonomy), severity, status, and what it affects.

**How to navigate:** follow `affects:` to the rule, file, or spec involved; follow
`related_specs:` to the governing specs. IDs are monotonic and never reused.
- [B-053](B-053-disabled-account-login-issues-token.md) — both login routes signed in a disabled (deleted, withdrawn or rejected) account (incomplete-rule, high, resolved)
- [B-023](B-023-auth-register-api-never-existed.md) — auth.register is spec-ahead-of-code: no JSON register route exists (layer-drift, medium, resolved in PAD-210)
- [B-029](B-029-ios-composer-band-under-keyboard-and-round-send.md) — iOS composer band under the keyboard, round send button, web send button shorter than the input (incomplete-rule, low, resolved)
- [B-080](B-080-nightly-prod-backup-is-a-no-op.md) — the nightly prod backup cron reads an unset variable, runs pg_dump on the host and uploads to an empty bucket; prod has had no automatic backup (incomplete-rule, high, open — unverified on the VM)
- [B-081](B-081-a-filled-vacancy-kept-inviting.md) — a filled vacancy kept inviting: four hand-written closers disagreed, `queued` invitations survived every one but the reconciliation's, and the waiting-list placement retired nothing; the same shape is on `origin/main` (PAD-317, resolved)
- [B-041](B-041-respond-waiting-list-no-offer-check.md) — any player could queue on any class by id: respond_waiting_list never checked for an offer (incomplete-rule, high, resolved in PAD-222)
- [B-069](B-069-postgres-container-published-on-all-interfaces.md) — the Postgres container is published on 0.0.0.0:5432 and only the firewall keeps it off the internet; loopback + levelup_net in the startup script, live VM re-created by hand (incomplete-rule, low, triaged in PAD-292)
- [B-031](B-031-serialize-user-leaks-contact-fields.md) — the users list, the messageable picker and the public activation lookup returned email and phone to anyone (incomplete-rule, high, resolved in PAD-227)
- [B-070](B-070-import-analysis-streams-outside-the-request-context.md) — the AI import analysis streamed outside the request context, its coach-level lookup failed into a warning and the import ignored the coach's existing levels (incomplete-rule, medium, resolved in PAD-293)
- [B-030](B-030-needs-you-later-button-does-nothing.md) — "Mais tarde" on a needs-you empty-seats card did nothing on either shell; now a 24h server-side snooze (incomplete-rule, medium, resolved)
- [B-032](B-032-student-upcoming-kpi-counts-confirmed-only.md) — student "Upcoming lessons" KPI counted confirmed presences and read 0 above a populated schedule; now the schedule's own count (layer-drift, medium, resolved in PAD-235)
- [B-033](B-033-bulk-validate-guards-only-the-first-class.md) — web bulk validate guarded only the first class of the run; now the in-flight set (incomplete-rule, low, resolved in PAD-191)
- [B-017](B-017-attendance-invited-flag-not-a-reminder-signal.md) — "Reminder sent" badge was gated on Presence.invited (roster membership); now on reminderSentAt derived from the reminder/invite messages (layer-drift, medium, resolved in PAD-199)
- [B-042](B-042-verify-screen-shows-resend-cooldown-as-an-error.md) — the verify-email screen printed RESEND_TOO_SOON as a red error on web and iOS; now only a countdown (incomplete-rule, medium, PAD-250)
- [B-043](B-043-transactional-mail-points-at-prod-and-missing-brand-assets-answer-200.md) — mail images pointed at prod in every environment; a missing /brand/* asset answered 200 text/html (incomplete-rule, medium, PAD-251)
- [B-044](B-044-ios-verify-screen-has-no-way-to-paste-the-code.md) — the iOS verify-email screen had no way to paste the code (incomplete-rule, high, PAD-251)
- [B-045](B-045-dashboard-validation-card-dead-link-and-mismatched-count.md) — coach dashboard "aulas por validar" card linked to a non-existent /validations route and counted presences over a rolling window while the tab counted classes per week; now one helper, one count endpoint, one week (layer-drift, high, resolved in PAD-201/PAD-190)
- [B-050](B-050-verified-coach-email-change-skips-verify-screen.md) — a coach whose email was already verified changed it in Settings and was sent straight back from the code screen on web and iOS; the save now refreshes the signed-in user first (layer-drift, medium, resolved in the 2026-09-10 batch)
- [B-034](B-034-activation-route-open-by-sequential-id.md) — activation routes open by sequential user id: anyone could set any inactive account's password; now a per-account HMAC secret in the link, status check, narrowed fields (incomplete-rule, critical, PAD-254)
- [B-035](B-035-deleting-a-level-deletes-the-roster.md) — deleting a coach level deleted every player at that level with their notes and evaluations, or 500'd; now unassigns via SET NULL FKs + a delete service (incomplete-rule, critical, PAD-255)
- [B-059](B-059-batch-migrations-abort-on-rows-referencing-deleted-records.md) — PAD-207's reminder backfill inserted a row for a deleted class and aborted the staging deploy (324 restarts, API 502); PAD-255's FKs had the same blind spot; both now tolerate references to rows that are gone (missing-criterion, high, resolved)
- [B-036](B-036-coach-current-club-returns-oldest.md) — Coach.current_club returned the oldest club, not the most recently joined; undated memberships must rank oldest on Postgres too (missing-criterion, medium, resolved in PAD-266)
- [B-054](B-054-model-migration-index-drift.md) — nine model-versus-migration differences on a clean database; `flask db migrate` would drop the two partial unique pending-request indexes (layer-drift, high, open)
- [B-056](B-056-declined-student-reinvited-same-round.md) — a student who declined an invitation was re-invited in the same round, so the spot never reached anyone else (missing-criterion, medium, resolved)
- [B-051](B-051-capacity-and-vacancy-check-then-write-without-a-lock.md) — capacity, one-winner-per-vacancy and materialisation were check-then-write with no row lock (incomplete-rule, high, resolved in PAD-261)
- [B-037](B-037-account-deletion-leaves-the-future-behind.md) — account deletion left enrolments, reminders, pushes, waiting-list credits, legacy login and roster listings behind (missing-dev-spec, medium, resolved in PAD-268)
- [B-038](B-038-privacy-policy-omits-ai-import-processor.md) — privacy policy does not name the AI import processor (OpenRouter) or the EEA transfer (layer-drift, medium, open — owner action)
- [B-052](B-052-editor-registry-traps.md) — editor registry traps: Message under "lessage" (404), DeviceToken and LessonInstanceTraining schema 500s; keys are now the lowercased class name, pinned by a registry walk (missing-criterion, low, resolved in PAD-280)
- [B-046](B-046-lesson-instance-occurrence-not-unique.md) — nothing in the database stops two lesson instances for the same occurrence; the unique constraint needs a duplicate merge first (incomplete-rule, medium, resolved in PAD-303)
- [B-049](B-049-profile-rows-orphaned-when-a-user-is-deleted.md) — players.user_id / coaches.user_id nullable and non-unique; deleting a user orphaned its profile and crashed serializers (incomplete-rule, high, triaged in PAD-260)
- [B-055](B-055-updated-at-local-time-and-never-bumped.md) — updated_at stamped in local time by save() and never bumped by a plain commit; token_blocklist wrote an aware datetime into a naive column (test-defect, low, resolved in PAD-273)
- [B-058](B-058-event-end-crosses-utc-midnight.md) — a class crossing UTC midnight vanished from both dashboards: `_event_end` joined the start date to the end time (missing-criterion, medium, resolved)
- [B-065](B-065-ios-day-sheet-without-corners-or-shadow.md) — the iOS calendar day sheet rendered flat: `shadow-lg` casts downward and `overflow-hidden` clips a view's own shadow on iOS, so the 20pt corners had nothing to show against (missing-criterion, low, resolved in PAD-286)
- [B-060](B-060-client-date-parsing-and-utc-presets.md) — client dates: bare dates parsed as UTC (wrong weekday west of UTC), presets and weeks on the UTC day, a Hermes-unsafe parse in the decline guard (incomplete-rule, medium, resolved)
- [B-066](B-066-clients-judge-has-it-started-on-device-time.md) — web and iOS compared Lisbon wall-clock class times, deadlines and windows with the device clock, so "started", "past", "next", "today" and late-cancel were wrong on any device outside Lisbon; lisbonNow()/isClubToday() in @levelup/config (incomplete-rule, medium, resolved in PAD-295)
- [B-067](B-067-android-select-options-are-untappable.md) — no select option could be tapped on Android: the portal wrapper shrink-wrapped to 0x0 around an absolutely positioned list, and Android neither hit-tests nor exposes children outside a parent's bounds (incomplete-rule, high, resolved in PAD-304)
- [B-089](B-089-android-dialog-layer-renders-without-its-component.md) — on Android a portal dialog can be drawn with no React component behind it, or dimmed with no content at all; three symptoms, one family, tracked as PAD-314 and blocking for the Play track (incomplete-rule, high, open)
- [B-057](B-057-removing-a-student-deletes-their-history.md) — removing an active student by their only coach deleted their Player record, presences and level history; now only the roster link goes, placeholders per PAD-260 rule 3, every removal audited (layer-drift, high, resolved in PAD-274)
- [B-061](B-061-editing-a-level-writes-no-history.md) — editing a level wrote no history; Player.level dead (PAD-270, resolved)
- [B-062](B-062-deletion-audit-index-not-declared-on-the-model.md) — PAD-274 migration created ix_deletion_audit_entity but the model did not declare it; the drift gate skipped the batch-3 backend deploy (incomplete-rule, high, resolved; PAD-299 adds flask db check to CI)
- [B-068](B-068-nested-app-context-orphans-outer-session.md) — a nested app context removed the outer context's session, which sat idle in a transaction until the collector found it; the Postgres CI job hung on the next TRUNCATE (incomplete-rule, high, resolved in PAD-291)
- [B-078](B-078-needs-you-reply-rows-do-not-open-the-message.md) — "Precisa de ti" reply rows sent `/messages?conversationId=`, which web ignored and iOS mapped to the Messages tab; now `/messages/<id>` on both shells (incomplete-rule, medium, resolved in PAD-284)
- [B-074](B-074-a-refused-return-is-rendered-as-a-decline.md) — `respondToReminder`'s declared answers outgrew the server's, so a student who asks to come back and is refused is recorded as having DECLINED: the mobile bubble writes "no" silently, the mobile dashboard toasts "declined" as a success. A wrong answer, not a missing one (PAD-315 fixes it)
- [B-077](B-077-counter-proposal-dead-ends-for-the-student.md) — the coach's counter-proposal reached the student as a chat question with nothing to press, and the student could not propose another time on any layer (incomplete-rule, high, resolved in PAD-281)
- [B-063](B-063-alembic-walk-in-the-test-session-disables-loggers.md) — migrations/env.py fileConfig disabled every existing logger; #208's mid-session migration test silenced push_sender and three #212 tests went red only on Postgres in the batch (incomplete-rule, medium, resolved; env.py keeps existing loggers)
- [B-079](B-079-e2e-load-flakes.md) — the E2E load-flake list: specs green alone that fail under batch-time load ≥100, each with its alone-pass evidence and its settle-wait pin or "load, no pin" (test-defect, low, triaged in PAD-300)
- [B-071](B-071-capacity-under-counts-when-enrolment-stores-disagree.md) — per-occurrence enrolment lived in three tables written on different paths; capacity under-counted when they disagreed (incomplete-rule, high, resolved in PAD-259)
- [B-072](B-072-vacancies-never-close-when-the-class-fills.md) — a vacancy only closed through the engine; any other way of filling the spot left it open and the engine kept inviting for a full class (incomplete-rule, medium, open; PAD-271 M4 reconciles)
- [B-090](B-090-the-deploy-cron-step-wiped-the-crontab.md) — the deploy's cron step wiped the prod crontab (`grep -v` matched nothing, exited 1, killed the subshell under `set -e`, so `crontab -` got an empty document) and never read it back, so it reported success for months; its failure also skipped the frontend deploy (incomplete-rule, high, PAD-353)
- [B-091](B-091-backup-restore-check-died-on-its-first-run.md) — `backup.sh restore-check` died on its first run (`local a=x b="$a"` under `set -u`), and three more never-run paths were broken too: the empty-bucket message never showed, a failed prune was silent, and a Discord failure payload with a quote in it was invalid JSON; all four now driven end to end by stub docker/gsutil/curl tests (incomplete-rule, medium, resolved 2026-09-16; PAD-296)
- [B-096](B-096-deleting-a-materialised-one-off-resurrects-it-without-its-attendance.md) — deleting a materialised one-off deleted only its instance; the parent Lesson re-projected the occurrence with its attendance gone while the API said `deleted` (incomplete-rule, high, PAD-335)
- [B-097](B-097-a-materialised-occurrence-carries-two-reminder-jobs.md) — a materialised occurrence carried `reminder_lesson_<lesson>_<date>` and `reminder_<instance>` firing at the same instant; with `reminder_count ≥ 2` students got two reminders in one minute, and every restart/day/settings save re-created the pair (incomplete-rule, high, PAD-347)
- [B-098](B-098-an-empty-level-leaves-a-dangling-de-in-portuguese-templates.md) — every pt default puts `{level}` after "de", so a level-less class read "aula de esta quarta-feira"; rule 7 wrongly claimed an empty string keeps the text grammatical (wrong-rule, medium, PAD-346)
- [B-099](B-099-the-backend-ci-budget-had-no-headroom-under-load.md) — the 30-minute backend CI budget cancelled a healthy sqlite run at 96% under runner load (27m30s sibling passed by 91 s); budget raised to 45 with the real duration stated (incomplete-rule, medium)
- [B-101](B-101-e2e-specs-left-shared-state-for-their-shard-mates.md) — specs left classes, accepted class requests and a debug reminder class in the shared E2E DB; removing a class never closed its request and a materialised one-off class re-projected its Lesson, so booking counted 2–3 accepted rows and the academy class fell off the 5-row schedule in shard 1 (test-defect, medium, resolved in PAD-341)
- [B-102](B-102-field-availability-messages-render-backend-english.md) — `check_field_available` answers 409 with English prose ("This email is already taken") that fields.ts passes through verbatim, so the add-player warning is English on the pt app on web and mobile; the E2E spec asserted the English literal and the rendered-text guard cannot see copy that is in no locale file (incomplete-rule, low, resolved in #327)
- [B-111](B-111-evaluation-save-fabricated-midpoint-scores.md) — both evaluation forms seeded every category at its midpoint and posted all of them, so a save fabricated grades for untouched categories and re-posting an unchanged score moved `evaluatedAt` (missing-criterion, high, resolved in PAD-337)
- [B-112](B-112-schedule-row-title-collapsed-on-desktop.md) — the dashboard's Next 7 days switched to its fixed-column row at the viewport's `lg`, but the list sits in a ~530px column, so every class title got 66px; the row now follows the list's own width (incomplete-rule, medium, resolved in PAD-336)
- [B-113](B-113-class-screen-requests-an-empty-calendar-range.md) — the iOS class screen called `useCalendarEvents("", "")` until the instance loaded, so every class open sent `GET /calendar?from=&to=` and logged a 400; the shared hook now waits for both ends (missing-criterion, low, resolved in PAD-348)
- [B-092](B-092-no-pr-check-ran-the-frontend-unit-suites.md) — no PR check ran vitest, so the three E2E guards (rendered-text, role-name, bilingual alternation) only failed on the assembled batch; a `unit` job now runs `npm test` on every frontend PR (incomplete-rule, medium, resolved 2026-09-16)
- [B-114](B-114-maestro-cannot-observe-ios-board-playback.md) — iOS ▶ AUTO looked dead to Maestro (no playback-ball, label still ▶) but runtime logs show it plays on schedule; XCUITest waits for the UI to settle, so mid-playback is unobservable on iOS (test-defect, low, triaged)
- [B-100](B-100-a-quiet-hours-test-failed-by-the-hour-of-the-day.md) — a PAD-331 test pinned "now" to 02:00 but seeded its class off the real clock, so it failed between 00:00 and 08:00 club-local; the class is now pinned relative to the same instant (test-defect, medium, resolved 2026-09-17)
- [B-103](B-103-e2e-assertions-on-rendered-copy.md) — E2E assertions on rendered copy break when a string is translated and can pass while the feature is broken (English literal on the pt app matches only untranslated text); the ratcheted guard, what it still cannot see (copy in no locale file, <3-char text, placeholder/label regexes), and the conversion pattern (test-defect, medium, triaged; PAD-320)
- [B-127](B-127-two-test-files-were-collected-by-no-runner.md) — PAD-348's regression test for B-113 (`packages/hooks/src/useCalendarEvents.test.tsx`) and PAD-223's `e2e/scripts/test_seed_dates.py` were collected by no runner or lane, so B-113 was `resolved` on a test never executed; both are now collected, both pass (the first fails with the fix removed), and a guard on each side fails when any test file is collected by zero runners, reading "collected" from the runners themselves (incomplete-rule, medium, resolved 2026-09-21)
- [B-128](B-128-a-week-view-test-expected-the-next-class-filled.md) — US-247-2 expected the filled coach colour for the `next` class, which `calendar.mobile-views` rule 5 says is outlined; the component is right; on the real clock the mocked Tuesday class is `next` only from Monday 09:30 to Tuesday 11:30, so the spec was red ~26 hours a week; the test now pins the browser clock and asserts past, next and scheduled at once (test-defect, medium, resolved 2026-09-21)
- [B-125](B-125-renaming-an-evaluation-category-strands-its-scores.md) — the settings upsert is keyed on name and ids are never sent, so a rename inserts a new category and the old one keeps every score; reproduced and pinned in PAD-362, not fixed on the legacy endpoint (App Store 1.1.0 posts that body); rename-by-id on PAD-364's new PATCH (incomplete-rule, medium, triaged)
- [B-126](B-126-the-server-enforces-no-evaluation-score-range.md) — `evaluations.entries` rule 4 is enforced only by client controls: 99, -3 and 2.5 are written on a 0-10 scale, and the save is not atomic; reproduced and pinned in PAD-362; the range check lands on PAD-364's new write endpoint only (test-defect, medium, triaged)
- [B-136](B-136-the-form-layer-drops-a-zero.md) — `Field.set_value` keeps only truthy values, so a numeric 0 never arrives: a 0-10 evaluation scale is stored 1-10 (the echo says 0), the import drops it too unless it is the string "0", and a numeric score of 0 fails the NOT NULL column; found and pinned in PAD-362, not fixed without an owner decision (incomplete-rule, medium, triaged)
- [B-145](B-145-evaluation-score-written-into-another-coachs-category.md) — `POST /app/add_evaluation_entry` never checked whose `categoryId` it was: another coach's id wrote a cross-coach row with a 200, an unknown id was a 500 after the earlier scores were kept; new writes are closed by PAD-370 (only the caller's own categories are accepted, anything else is ignored); rows already written, if any, await a production count (incomplete-rule)
- [B-135](B-135-a-class-requests-hold-outlives-the-request.md) — only withdraw / decline / accept released a class request's hold; both editor delete routes, a Player/Coach delete that cascades the request away, and account deletion (the hold kept the deleted student's real name) left a ghost block on the coach's calendar; rule 18 "a hold never outlives its request", account deletion closes open requests silently (incomplete-rule, medium, triaged; PAD-360)
- [B-137](B-137-the-legacy-evaluation-save-is-not-atomic.md) — `add_evaluation_entry_service` commits one score at a time, so when a later score in the same body fails the earlier ones stay written (sqlite and Postgres); found in PAD-362, pinned, not fixed on the frozen legacy endpoint; PAD-364's new write is atomic from the start (incomplete-rule, medium, triaged; PAD-368)
- [B-138](B-138-a-moved-occurrence-of-a-hold-is-a-clone-nothing-releases.md) — moving or deleting ONE occurrence of a weekly class-request hold cloned the block; the clone copied the hold's title, no request pointed at it and no release path found it, so it outlived the request under the student's name; a `single`/`future` change to a live hold is now refused (`409 HOLD_OCCURRENCE_LOCKED`) and `requestHoldOf` tells the shells not to offer it (incomplete-rule, medium, resolved in PAD-372)
- [B-155](B-155-settings-sections-reload-and-undo-edits.md) — five Settings sections loaded in an effect with `t` in its deps; `t` changes identity when the language settles (every page load on web), the effect re-ran and its reload silently undid an unsaved edit; found by a release run going red on a spec reported green (incomplete-rule, high, resolved in PAD-392)
- [B-139](B-139-single-occurrence-change-drops-the-rest-of-the-series.md) — deleting or moving ONE occurrence of a recurring calendar block dropped every later one (`_split_block` looked for the resume point after shortening the series), and did nothing at all to the FIRST occurrence (`_next_occurrence_after` searched from that date's midnight) (test-defect, high, resolved in PAD-371)
- [B-143](B-143-attendance-readers-count-every-coachs-classes.md) — `_attendance_stats(_for)` behind the `justified_absences` / `attendance_rate` bars, the wave ranking, the approval-queue pick and the invite simulation, and the two readers filling the manual-invitation dialog's absence groups, counted the student's presence rows with EVERY coach; scoped to `coach_instance_ids(coach_id)` like `_unjustified_absence_count`, and the `justified_absences` bar compares the scoped count (incomplete-rule, medium, resolved in PAD-382)
- [B-150](B-150-block-edited-to-one-off-keeps-repeating.md) — a recurring block edited to a one-off answered `isRecurring: false` but kept its rule (the form layer drops the empty value), so the feed kept serving it weekly and a student's one-day unavailability kept excluding them from invitations every week; an edit that omitted the key also flipped the flag (incomplete-rule, high, resolved in PAD-377)
- [B-158](B-158-ios-competency-manager-has-no-harness-proof.md) — the wave-3 assembly of #361 (deletes the iOS evaluation-categories section) and #375 (had its harness test) left the competency manager that replaced it with no harness proof that an unsaved edit survives a new `t` or a list refetch; four cases on the PAD-393 harness, mutant-proven, no code change (missing-criterion, low, resolved in PAD-399)
- [B-152](B-152-present-row-keeps-the-absence-justification.md) — correcting "absent, justified" to present left the justification on the row (every client omits the key, and the form layer leaves an absent key alone); the invitation engine counts rows by `justification` without checking `status`, so corrected rows kept counting as make-ups owed; code fixed in PAD-381; the post-deploy production re-count was 0 of 4,479, so no cleanup was owed (incomplete-rule, medium, resolved)
- [B-129](B-129-a-push-sender-test-timed-the-caller-against-a-wall-clock-budget.md) — `test_a_system_message_returns_before_the_push_round_trip` asserted `elapsed < 0.25 s` against a 0.3 s fake push and failed on a slow Postgres runner (0.383 s) while the same code passed twice; the claim is an ORDER, now proven with an Event the test releases only after the engine returns — no budget (test-defect, medium, resolved 2026-09-22)
- [B-130](B-130-the-consent-countdown-read-the-clock-twice.md) — a minor's `POST /api/auth/register` stamped the consent link with one `utcnow_naive()` read and computed `resendAvailableInSeconds` from a second, so a whole second between them (a slow CI runner, #361's sqlite job) answered 59 for the 60 s cooldown; one `now` now threads handler → `register_user_service` → `start_consent` → `pending_body`, pinned by a clock that moves on every read (bug, low, resolved 2026-09-22)
- [B-131](B-131-a-debug-endpoints-test-class-was-never-removed.md) — `debug_schedule_reminder_test` created an "E2E Auto-Reminder Test" class at club-now + 48h and nothing removed it; on a Tuesday the leak landed on the seed's "next Thursday" slot and, with 3+ cards overlapping, `CalendarGrid.tsx`'s compact rule hid the filled/capacity line, failing `participant-count-effective.spec.ts`'s `toContainText("1/4")` on what looked like a wrong count; fixed with a same-gated cleanup route (`.../cleanup`, built on `remove_class_service`) called by every caller's `afterAll` (test-defect, low, resolved 2026-09-22)
- [B-140](B-140-add-window-creates-a-zero-length-window.md) — the working-hours editors' "add window" appended `[last end, 22:00]`, which on an untouched day is 22:00–22:00 and is refused on save; one shared `addWorkingWindow()` now always gives a saveable day or disables the control (incomplete-rule, medium, resolved in PAD-361)
- [B-141](B-141-working-hours-controls-leave-the-15-minute-grid.md) — the working-hours time controls could leave rule 2's 15-minute grid (iOS wheel in 1-minute steps; web's `step={900}` does not stop a typed 22:07 nor flag it) and the save was refused; the shared picker takes an opt-in `minuteInterval`, web snaps on blur (incomplete-rule, medium, resolved in PAD-369)
- [B-142](B-142-working-hours-cannot-end-at-midnight.md) — the server accepts a working window ending 24:00 but neither editor can express it, so the latest saveable end is 23:45 (`snapToGrid` is "nearest, capped at 23:45" and says so) (incomplete-rule, low, triaged; PAD-379)
- [B-156](B-156-mobile-has-no-component-test-harness.md) — the mobile vitest project could not mount a component, so iOS behaviour was settled by "same shape as web"; `renderNative` + host primitives in the react-native stub + a transform for @rn-primitives' raw-JSX .js, first case red on staging's working-hours section and green on #367's (test-defect, medium, resolved in PAD-393)
- [B-171](B-171-weekly-qa-reset-migrates-with-the-main-checkout.md) — the weekly QA reset resolved the backend and seed from its own path under the gitignored docs/, so levelup_qa was built by the parked main checkout (46 migrations vs staging's 84); now a tracked script that takes QA_CHECKOUT + QA_COMMIT and refuses before touching the DB (incomplete-rule, high, resolved in PAD-398)
