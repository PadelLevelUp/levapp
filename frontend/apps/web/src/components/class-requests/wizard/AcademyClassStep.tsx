/**
 * PAD-358's step, joining an academy class. STUB: Session C's
 * feature/pad-358 replaces this file's contents (same path, same signature).
 */
export function AcademyClassStep({ coachId, onDone }: { coachId: string; onDone: () => void }) {
  void coachId;
  void onDone;
  return <div data-testid="academy-class-list" />;
}
