import { View } from "react-native";

/**
 * PAD-358's step, joining an academy class. STUB, as on web: the iOS step lands
 * on feature/pad-358, which replaces this file's contents (same path, same props).
 */
export function AcademyClassStep({ coachId, onDone }: { coachId: string; onDone: () => void }) {
  void coachId;
  void onDone;
  return <View testID="academy-class-list" />;
}
