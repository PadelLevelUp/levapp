/**
 * Bottom padding for the lists the floating add buttons sit over — the Dia
 * card list and the Semana / Mês day sheet (calendar.mobile-views rule 18).
 *
 * The coach's button stack rises 144pt above the screen's bottom edge (Add
 * event: `bottom-24`, 48pt tall, over Add class: `bottom-6`, 56pt tall). 24pt
 * more, so the last card clears the stack with a gap.
 */
export const FAB_CLEARANCE = 168;
