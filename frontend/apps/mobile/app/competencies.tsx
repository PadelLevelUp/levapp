import { CompetencyManagerScreen } from "@/features/evaluations/competency-manager/competency-manager-screen";

/** "Gerir competências" (PAD-373): pushed by `openCompetencyManager(router)` — COMPETENCY_MANAGER_ROUTE.mobile. */
export default function CompetenciesRoute() {
  return <CompetencyManagerScreen />;
}
