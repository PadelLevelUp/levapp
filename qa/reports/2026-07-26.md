# Weekly QA Report — 2026-07-26

**Run type:** ✅ ENFORCEMENT (baseline `baseline-latest.json` from 2026-07-21 exists → P0/P1 auto-filed).
**Environment (isolated):** DB `levelup_qa`, backend port **5003** (conventional 5002 held by unrelated Docker `com.docker.backend`; QA isolation is guaranteed by the `levelup_qa` DB, so the port number is immaterial). Web frontend on 8080 → 5003. Phase-1 Playwright ran its own `levelup_test`:5001 webServer (expected, separate).
**Repos (checked-out feature branches, each `main + 1` commit — see "Branch gate" below):** backend `d52dda3` (`feature/lvl-pad-93`, main = `58f383a`), frontend `17e52c2` (`fix/mobile-dialog-width-collapse`, main = `74b2ddf`).

**Coverage this run**

| Phase | Web | iOS |
|---|---|---|
| 1 — Regression | ✅ Playwright **187/188** (0 failed, 1 skip) — **0 regressions** | ❌ not run (scheduled-run computer-use limitation) |
| 2 — Exploratory journeys | ✅ P0 + P1 journeys walked; last week's 3 P1s **verified fixed** | ❌ not run |
| 3 — Design critique | ✅ light + **dark** (desktop) + **mobile 375px** (new coverage) — all key screens | ❌ not run |

> **iOS not run this cycle.** iOS journeys, the Maestro regression, and the iOS design critique require driving the Simulator via computer-use, whose macOS Accessibility/Screen-Recording grant cannot be shown during a scheduled run. Environment limitation, not a product defect. iOS enters the rotation on the first **interactive** run. (Same as 2026-07-21 and 2026-07-12.)

---

## 0. Headline

- **Clean, healthy week. Zero new P0/P1 findings → nothing auto-filed.**
- **All three of last week's P1 functional bugs are VERIFIED FIXED and now guarded by tests:**
  - **PAD-64** (attendance 404 on recurring classes) — re-walked the exact flow (open recurring occurrence → Mark attendance → Present → Confirm): saved cleanly, no error. New passing spec `attendance-save.spec.ts::PAD-64`.
  - **PAD-65** (recurring occurrence resurrects after edit+single-delete) — new passing spec `recurring-occurrence-delete.spec.ts::PAD-65`.
  - **PAD-66** (unread never clears ~1h due to local-time `sent_at`) — deterministically re-verified via API: app-sent message stamped correct UTC (`19:30:15+00:00`), unread went 2 → **0** immediately on read. Code confirms `sent_at = utcnow_naive()`.
- **Regression suite grew +24 specs** (PAD-64/65/71/72/74/76/79/80/81/84 coverage landed on main) with **0 pass→fail**. The previously-known stale `ticket-pad-24` failure is now **fixed** (test updated to click the PAD-58 confirm dialog).
- **Dark mode solid app-wide** (PAD-57 holds) and **mobile (375px) responsive is clean** — the mobile/tablet pass that was cut short last week now covered with no defects found.

---

## Branch gate (why enforcement filing is still safe)

Both repos were parked on active, **unmerged** feature branches (a concurrent "implement tickets" session's work), each exactly **1 commit ahead of main, 0 behind**. The baseline was built on `main`, so any finding must be checked against main before filing. The delta was tiny and fully characterized:

- **Frontend `17e52c2` (PAD-102)** touches **only `apps/mobile/`** (mobile dialog width). The web app (`apps/web`) is **byte-identical to main** → every web regression / journey / design finding this run is exactly what ships on main. Safe to file.
- **Backend `d52dda3` (PAD-93)** touches `messaging_service.py` (only `create_conversation_service` `is_group` logic), `calendar_service.py` (+5), `user_service.py` (+26), `conversations*.py`, + a backfill migration. Only functional bugs in **new-conversation creation / calendar / user** areas would need a main cross-check. The messaging *read/unread* path I exercised is shared code unaffected by PAD-93, so the PAD-66 verification is clean.

**Net:** zero new P0/P1 were found, so the gate never had to block a filing. No worktree was needed (avoided the documented Poetry/npm/iCloud worktree failure modes); the checked-out branches and their untracked in-flight files (`apps/mobile/app/availability.tsx`, `appstore-screenshots/`) were left untouched.

---

## 1. Regressions (Phase 1 — Playwright, own `levelup_test`:5001 webServer)

Result: **187 passed, 0 failed, 1 skipped** (188 total). Diff vs baseline (2026-07-21: 163 passed / 1 failed / 1 skipped):

- **Pass→fail regressions: 0.** ✅
- **Fail→pass: 1** — `loading-states/ticket-pad-24-loading-states.spec.ts::class delete shows loading state…` (last week's known stale-test failure) now **passes** — the test was updated to click through the PAD-58 confirm dialog. → the `known-failures.md` PAD-24 entry is now **stale** (see §4).
- **New specs: 24, all passing** — coverage that landed on main this week:
  - `schedule-calendar/attendance-save.spec.ts` (PAD-64 fix + US-20 materialized instance)
  - `schedule-calendar/recurring-occurrence-delete.spec.ts` (PAD-65 fix)
  - `schedule-calendar/participant-count-effective.spec.ts` (PAD-71), `guest-list-dedupe.spec.ts` (PAD-72)
  - `dashboard/student-dashboard-kpi.spec.ts` (PAD-76 Missed-KPI 404), `dashboard/upcoming-class-deeplink.spec.ts` (PAD-79)
  - `players/add-to-classes.spec.ts` (PAD-80), `settings/profile-persistence.spec.ts` (PAD-81), `settings/coach-levels-ordering-hint.spec.ts` (PAD-84)
  - `notification-engine/manual-notify-selection.spec.ts` (PAD-74), `notification-engine/blank-template-fallback.spec.ts`
- **New skip (coverage note, not a regression):** `settings/coach-settings.spec.ts::US-69: skill levels management is accessible from settings` went **passing → skipped**. It uses a conditional `test.skip(true, "Skill levels section not found in settings")` — it silently self-skips when the section isn't located where the test looks. This masks coverage rather than failing loudly. **Test-health follow-up (not a product ticket)** — see §4. Skill-levels management itself is present and functional (verified in the Settings walk; PAD-84 ordering-hint specs pass).

---

## 2. Functional journeys (Phase 2 — exploratory walk, Chrome MCP vs `localhost:8080`)

All P0 + P1 web journeys walked. **No new P0/P1 functional bugs.**

| Journey | Pri | Result | Notes |
|---|-----|--------|-------|
| web-auth-happy-path | P0 | ✅ PASS | Login → dashboard; session persists on reload + direct-nav; logout → `/auth`; protected `/dashboard` redirects to `/auth`. |
| web-auth-wrong-password | P0 | ✅ PASS | Clear error toast ("Falha ao iniciar sessão / …inválidos"), no crash, form usable, no console error, retry succeeds. |
| web-messaging-sse-unread | P0 | ✅ PASS | **PAD-66 verified fixed** (see §0). UI: message renders at correct local time (08:30 PM), nav unread badge cleared. 8 messaging Playwright specs green. |
| web-coach-player-lifecycle | P1 | ✅ PASS | Players list (search/sort/data-quality alerts), detail page, Strengths & Weaknesses + Info all render. Add-player covered by passing specs. |
| web-calendar-class-lifecycle | P1 | ✅ PASS | **PAD-64 verified fixed** — attendance saved on a recurring occurrence. Week nav clean; recurring class on Tuesday (PAD-59 holds); class-detail controls all present. |
| web-evaluation-notes | P1 | ✅ PASS (functional) | Evaluation saves + persists + renders; English toast. Two **persisting P2s** below. |
| web-settings-language-theme | P1 | ✅ PASS | Theme light→**dark** applies app-wide + persists across nav/reload (PAD-57). Language switch covered by passing `language-preference.spec.ts::PAD-40`. Coach-levels ordering hint (PAD-84) present. |
| web-empty-state-new-account | P2 | ⏭️ not walked (fresh-registration) | But purposeful empty states observed throughout: calendar "No classes scheduled", messages "Select a conversation", player "No evaluations yet" / "None recorded". No blank/crash states seen. |
| web-notifications-reminders | P2 | ✅ controls present | Notify + Remind buttons present in class detail; send-path covered by passing `reminder-flow.spec.ts` (US-REM-01..08). |

### Persisting P2/P3 functional findings (report only — unchanged from 2026-07-21, not re-filed)

| # | Sev | Area | Finding |
|---|-----|------|---------|
| FB-5 | **P2** | evaluations | Untouched score slider silently records a fabricated **6/10** ("Forehand 6/10" with no unset state). Confirmed again: saved an evaluation touching only a strength → a 6/10 Forehand score was persisted under the coach's name. |
| FB-6 | **P2** | evaluations | Saved evaluation shows **no author/timestamp** in the UI; the "persists with correct author/timestamp" journey expectation is structurally unmet (fields not surfaced). |
| FB-9 | P3 | auth | Wrong-password error is toast-only (bottom corner), no inline field error, password not cleared. |

---

## 3. Design findings (Phase 3 — rubric v1)

Overall the web UI remains **clean, consistent, and well-aligned** in **both** themes and at **all three breakpoints**. No P0/P1 design findings.

- **Dark mode (desktop): solid app-wide.** Selecting Dark applies dark surfaces to nav, cards, inputs, sheets, and the calendar grid across Dashboard, Players, Calendar, Messages, Settings — and **persists across navigation and reload**. No black-on-black, unstyled white cards, invisible text, or contrast failures found. (PAD-57 fix holds.)
- **Mobile 375px (new coverage — last week's gap): clean.** Bottom tab-bar nav appears; Dashboard KPI cards reflow to a tidy grid; Calendar collapses to a compact single-line header (PAD-58 holds) + week-strip + day-detail with a good empty state; Messages adapts to a single-pane list (no broken split-pane). **No horizontal body scroll or clipping observed** on any mobile screen checked.
- **Auth page** correctly inherits the persisted **theme (dark)** and **language (en)** after a session — the Portuguese render seen on a truly fresh browser is just the default-locale (pt) fallback with no stored preference.

Design findings (all ≤ P3 this week):

| # | Sev | Screen / platform | Dimension | Finding |
|---|-----|-------------------|-----------|---------|
| D-1 | P3 | global · web | i18n | Login **success toast renders in Portuguese** ("Bem-vindo! Iniciou sessão com sucesso.") over an otherwise-English app, on a fresh browser whose default locale is pt. i18n boundary leak at the pre-auth→post-auth moment. **Dedupe → PAD-48.** |

Per-screen rubric score blocks are in `baseline-latest.json` (`design_scores_web`). Web dark-mode + desktop screens scored ~5.0 across dimensions; mobile screens (dashboard/calendar/messages) scored 5 on responsive with no findings.

---

## 4. Other observations / follow-ups (report only — no product tickets)

- **known-failures.md housekeeping (done this run):** the `ticket-pad-24-loading-states` entry is now **stale** — the test was updated and passes. **Removed** from `known-failures.md` so a future genuine failure isn't masked.
- **Test-health follow-up (not a QA product ticket):** `settings/coach-settings.spec.ts::US-69` now conditionally self-skips (`test.skip(true, "Skill levels section not found in settings")`). Worth updating the locator so it asserts against the current Settings → Preferences → Coach Levels location instead of silently skipping. Added to `known-failures.md` iOS/notes as a coverage note.
- **Attendance-save coverage gap (from last week) is now closed** — `attendance-save.spec.ts` (materialized instance + recurring occurrence) landed. Good.
- **Dashboard "Revenue (est.)" KPI** still present — this is the subject of open **PAD-78** ("substituir Revenue por presenças por confirmar"), In Progress. Not a QA finding; not filed.
- **Persisting security/UX P2s from prior weeks** (logout token-not-revoked / FB-4, SSE token-in-URL / FB-12, end-time-before-start / FB-7, language-persist-without-Save / FB-8) were **not re-exercised in depth this run**; they remain in prior reports and none block a core flow. Re-verify next interactive run.

---

## 5. Week-over-week delta vs 2026-07-21

**Fixed since last week (verified this run):**
- **PAD-64** attendance 404 on recurring classes — ✅ attendance saves; guarded by new spec.
- **PAD-65** recurring occurrence resurrection — ✅ guarded by new spec.
- **PAD-66** unread never clears (local-time `sent_at`) — ✅ UTC stamp + immediate unread-clear verified via API; code confirms `utcnow_naive()`.
- Stale `ticket-pad-24` Playwright failure — ✅ test fixed (clicks PAD-58 confirm dialog).

**New this week:** none (0 new functional bugs, 0 new design findings above P3).

**Persisting:** FB-5 / FB-6 (evaluation P2s); D-1 i18n success-toast leak (→ PAD-48). i18n leak scope **narrowed** — the authenticated app + post-session auth page are now English; only the success toast + fresh-browser default remain pt.

**Coverage gained:** mobile 375px responsive pass (previously blocked); +24 regression specs on main.

---

## 6. Auto-filed tickets (P0/P1 only)

**None.** Zero new P0/P1 findings this run — clean enforcement week. P2/P3 remain in this report only; the one P3 i18n leak deduped into standing **PAD-48** (no comment added — already the standing i18n ticket and no behavioral change vs last week).

---

### Run metadata
- Rubric version 1 · Journeys version 1
- Web regression: 187/188 passed (0 failed, 1 conditional skip), Playwright `levelup_test`:5001
- Screens scored (web): light + dark desktop + **mobile 375px** (dashboard, calendar, players, messages, settings, auth)
- iOS: not run (scheduled-run computer-use limitation)
- Repos: backend `d52dda3` (feature/lvl-pad-93, main+1), frontend `17e52c2` (fix/mobile-dialog-width-collapse, main+1; web app == main)
- Report generated: 2026-07-26T19:36Z
