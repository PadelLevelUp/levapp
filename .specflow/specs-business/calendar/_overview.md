# calendar

## What this is

Business outcomes for the calendar domain.

## What it covers

- `calendar.coach-relies-on-calendar` — draft placeholder, to be split into persona journeys.

## Why it's grouped this way

Mirrors the developer-spec domain so every leaf's `implements:` link stays local to its domain.
