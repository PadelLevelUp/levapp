# messaging

## What this is

Business outcomes for the messaging domain.

## What it covers

- `messaging.coach-relies-on-messaging` — draft placeholder, to be split into persona journeys.

## Why it's grouped this way

Mirrors the developer-spec domain so every leaf's `implements:` link stays local to its domain.
