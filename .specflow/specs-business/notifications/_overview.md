# notifications

## What this is

Business outcomes for the notifications domain.

## What it covers

- `notifications.coach-relies-on-notifications` — draft placeholder, to be split into persona journeys.

## Why it's grouped this way

Mirrors the developer-spec domain so every leaf's `implements:` link stays local to its domain.
