# settings

## What this is

Business outcomes for the settings domain.

## What it covers

- `settings.coach-relies-on-settings` — draft placeholder, to be split into persona journeys.

## Why it's grouped this way

Mirrors the developer-spec domain so every leaf's `implements:` link stays local to its domain.
