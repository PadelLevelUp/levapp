# training

## What this is

Business outcomes for the training domain.

## What it covers

- `training.coach-relies-on-training` — draft placeholder, to be split into persona journeys.

## Why it's grouped this way

Mirrors the developer-spec domain so every leaf's `implements:` link stays local to its domain.
