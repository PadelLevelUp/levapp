# attendance

## What this is

Business outcomes for the attendance domain.

## What it covers

- `attendance.coach-relies-on-attendance` — draft placeholder, to be split into persona journeys.

## Why it's grouped this way

Mirrors the developer-spec domain so every leaf's `implements:` link stays local to its domain.
