# calendar — Calendar View & Blocks

## What this is

The calendar domain.

## What it covers

- `calendar.view` — implemented
- `calendar.blocks` — implemented
- `calendar.student-blockers` — implementing
- `calendar.drag-drop` — implemented
- `calendar.event-detail` — implemented
- `calendar.slot-click` — implemented
- `calendar.seasons` — implemented
- `calendar.mobile-views` — draft (Dia / Semana / Mês phone calendar on web and iOS, from the
  2026-09-08 design canvas; supersedes the phone parts of `calendar.view`)

## Why it's grouped this way

One domain of the LevelUp product, migrated 2026-09-03 from the legacy `specs/calendar/spec.md` (one file per domain) into one leaf per behaviour. Leaves sit directly under the domain — no capability folders yet.
