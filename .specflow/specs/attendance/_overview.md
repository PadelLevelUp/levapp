# attendance — Presence & Attendance Tracking

## What this is

The attendance domain.

## What it covers

- `attendance.presence` — implemented
- `attendance.confirm` — implemented
- `attendance.stats` — implemented
- `attendance.history` — implemented
- `attendance.absences` — draft
- `attendance.validation` — draft

## Why it's grouped this way

One domain of the LevelUp product, migrated 2026-09-03 from the legacy `specs/attendance/spec.md` (one file per domain) into one leaf per behaviour. Leaves sit directly under the domain — no capability folders yet.
